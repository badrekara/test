import { SafeAreaView, StyleSheet, Text, ScrollView } from "react-native";

const Home = () => {
   return (
       <SafeAreaView>
          <ScrollView>
            
          </ScrollView>
       </SafeAreaView>
   );
};

export default Home;

const styles = StyleSheet.create({});